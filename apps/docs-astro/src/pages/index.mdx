---
layout: ../layouts/DocsLayout.astro
title: Natura11y Astro Docs Spike
---

import { Accordion, AccordionItem, Button } from '@natura11y/react';

<section className="container narrow padding-y-5">
  <p className="font-size-sm text-transform-uppercase font-weight-bold margin-bottom-2">
    Astro docs spike
  </p>

  <h1 className="banner-headline margin-bottom-3">
    React components from the working source
  </h1>

  <p className="font-size-md">
    This page imports from <code>@natura11y/react</code>. Inside this Astro app,
    that package name is aliased to <code>packages/react/src/components/index.ts</code>,
    so edits to the working React components can show up here during docs development.
  </p>
</section>

<section className="theme-light padding-y-5">
  <div className="container narrow">
    <h2>Static React Component</h2>

    <p>
      This button is server-rendered by Astro from the React component source.
      It does not need browser hydration because it has no local interaction.
    </p>

    <Button
      tag="a"
      title="Source Button"
      linkUrl="#accordion-proof"
      iconEndHandle="arrow-right"
      utilities="theme-primary"
    />
  </div>
</section>

<section className="padding-y-5" id="accordion-proof">
  <div className="container narrow">
    <h2>Hydrated React Component</h2>

    <p>
      This accordion uses <code>client:load</code>, so Astro renders it from the
      same React source and then hydrates it for interaction.
    </p>

    <Accordion client:load defaultOpen="one" headingLevel={3}>
      <AccordionItem itemId="one" title="Same import shape">
        <p>
          The MDX import is still <code>@natura11y/react</code>, even though this
          spike points it at the local working source.
        </p>
      </AccordionItem>

      <AccordionItem itemId="two" title="Useful docs migration proof">
        <p>
          This would let the future Astro docs use real React components without
          keeping Gatsby-style duplicate docs components alive.
        </p>
      </AccordionItem>
    </Accordion>
  </div>
</section>
