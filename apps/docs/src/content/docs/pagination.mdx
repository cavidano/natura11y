---
category: a-z
type: component
title: Pagination
slug: pagination
bannerCreditName: Zdeněk Macháček
bannerCreditUrl: https://unsplash.com/@zmachacek
bannerImage: ./banner/hero-pagination.jpg
bannerAlt: A row of white Eurasian Spoonbills standing on top of a lush green field
description:
    Accessible pagination navigation for multi-page content using the pagination component.
relatedPages:
  - "Navigation"
  - "Button"
  - "Icon"
---

import Truncated from '../../components/examples/pagination/Truncated.astro';
import VisibleLabels from '../../components/examples/pagination/VisibleLabels.astro';

<Section id="introduction">

<h2 className="screen-reader-only">
    Introduction
</h2>

<TextBlock>

The pagination component lets users navigate across pages of content. It uses the `.pagination` class on a `<ul>` element and inherits base styles from the navigation system.

</TextBlock>

<AccessibilitySpotlight>

Wrap the pagination in a `<nav>` element with a unique `aria-label` to identify it as a navigation landmark.

Add `aria-current="page"` to the active page link so assistive technology can announce the current page.

Use `aria-label` on previous and next links when they contain only an icon. If visible text is present, the text is the accessible name and `aria-label` is not needed.

Add `aria-hidden="true"` to truncation items so screen readers skip them.

</AccessibilitySpotlight>

</Section>

---

<Section id="pagination">

<TextBlock>

## Pagination

Add `.pagination` to a `<ul>` element wrapped in a `<nav>`. Page links and previous/next controls use an `<a>` or `<button>` inside each `<li>`. Use `icon-more-horizontal` inside a plain `<li>` to indicate skipped pages. See Figure 1.

</TextBlock>

<FigureExample
    figureNumber="1"
    highlightedLines="1,3,4-8,16,21-23,27-31"
    code={`
    <nav aria-label="Pagination">

      <ul class="pagination">
        <li>
          <a href="#prev" aria-label="Previous page">
            <span class="icon icon-arrow-left" aria-hidden="true"></span>
          </a>
        </li>
        <li>
          <a href="#1">1</a>
        </li>
        <li>
          <a href="#2">2</a>
        </li>
        <li>
          <a href="#3" aria-current="page">3</a>
        </li>
        <li>
          <a href="#4">4</a>
        </li>
        <li aria-hidden="true">
          <span class="icon icon-more-horizontal" aria-hidden="true"></span>
        </li>
        <li>
          <a href="#9">9</a>
        </li>
        <li>
          <a href="#next" aria-label="Next page">
            <span class="icon icon-arrow-right" aria-hidden="true"></span>
          </a>
        </li>
      </ul>
    </nav>

  `}
>
    <Truncated />
</FigureExample>

<TableLineDescBlock targetFigure="1">

<TableLineDescRow targetFigure="1" lines="1">

The `<nav>` element with `aria-label` is required. It identifies the pagination as a navigation landmark. Use a unique label if more than one pagination component appears on a page.

</TableLineDescRow>

<TableLineDescRow targetFigure="1" lines="3">

The `.pagination` class on the `<ul>` element provides the bordered bar layout, item sizing, and dividers between items.

</TableLineDescRow>

<TableLineDescRow targetFigure="1" lines="4-8">

Previous links are placed first in the list. `aria-label` is required when the link contains only an icon.

</TableLineDescRow>

<TableLineDescRow targetFigure="1" lines="16">

Add `aria-current="page"` to the active page link. Omit the previous link when on the first page and the next link when on the last.

</TableLineDescRow>

<TableLineDescRow targetFigure="1" lines="21-23">

The truncation item indicates skipped pages. Place `icon-more-horizontal` directly inside the `<li>` with no `<a>` or `<button>` wrapper — it is purely visual. Add `aria-hidden="true"` to the `<li>` so screen readers skip it. Repeat wherever pages are omitted.

</TableLineDescRow>

<TableLineDescRow targetFigure="1" lines="27-31">

Next links are placed last in the list. `aria-label` is required when the link contains only an icon.

</TableLineDescRow>

</TableLineDescBlock>

</Section>

---

<Section id="visible-labels">

<TextBlock>

## Visible Previous and Next Labels

When previous and next links include visible text, the text provides the accessible name. In that case, `aria-label` is not needed.

</TextBlock>

<FigureExample
    figureNumber="2"
    markedText={['<span class="text">Previous</span>', '<span class="text">Next</span>']}
    code={`
    <nav aria-label="Pagination">

      <ul class="pagination">
        <li>
          <a href="#prev">
            <span class="icon icon-arrow-left" aria-hidden="true"></span>
            <span class="text">Previous</span>
          </a>
        </li>
        <li>
          <a href="#1">1</a>
        </li>
        <li>
          <a href="#2">2</a>
        </li>
        <li>
          <a href="#3" aria-current="page">3</a>
        </li>
        <li>
          <a href="#4">4</a>
        </li>
        <li aria-hidden="true">
          <span class="icon icon-more-horizontal" aria-hidden="true"></span>
        </li>
        <li>
          <a href="#9">9</a>
        </li>
        <li>
          <a href="#next">
            <span class="text">Next</span>
            <span class="icon icon-arrow-right" aria-hidden="true"></span>
          </a>
        </li>
      </ul>

    </nav>
  `}
>
    <VisibleLabels />
</FigureExample>

</Section>

---

<Section id="related-custom-properties">

<TextBlock>

## Related Custom Properties

Pagination styles can be customized by redefining the related CSS custom properties.

</TextBlock>

<RelatedCustomProperties
    figureNumber="3"
    properties={[
        {
            property: 'pagination-font-size',
            defaultValue: 'var(--font-size-md)',
            purpose: 'Font size of pagination items.',
        },
        {
            property: 'pagination-item-size',
            defaultValue: 'var(--button-target-size)',
            purpose: 'Minimum width and height of each pagination item, matching the accessible button tap target size.',
        },
        {
            property: 'pagination-current-text-color',
            defaultValue: 'currentColor',
            purpose: 'Text color of the active current page item.',
        },
        {
            property: 'pagination-current-background-color',
            defaultValue: 'var(--subtle-fill-2)',
            purpose: 'Background color of the active current page item.',
        },
    ]}
/>

</Section>
